---

- name: 清理 Mysql 资源(有命名空间)
  ansible.builtin.shell: |
    kubectl get {{ item.kind }} -n {{ namespace }} --no-headers -o custom-columns=":metadata.name" \
    | grep -E "^{{ item.name }}" \
    | xargs --no-run-if-empty kubectl delete {{ item.kind }} -n {{ namespace }} --ignore-not-found
  loop:
    - { kind: Deployment, name: "{{ mysql_name }}" }
    - { kind: StatefulSet, name: "{{ mysql_name }}" }
    - { kind: ReplicaSet, name: "{{ mysql_name }}" }
    - { kind: Job, name: "{{ mysql_name }}" }
    - { kind: Pod, name: "{{ mysql_name }}" }
    - { kind: Service, name: "{{ mysql_name }}" }
    - { kind: ConfigMap, name: "{{ mysql_name }}" }
    - { kind: Secret, name: "{{ mysql_name }}" }
    - { kind: PersistentVolumeClaim, name: "{{ mysql_name }}" }
  failed_when: false
  run_once: true


- name: 清理 Mysql 资源(无命名空间)
  ansible.builtin.shell: |
    kubectl get {{ item.kind }} --no-headers -o custom-columns=":metadata.name" \
    | grep -E "^{{ item.name }}" \
    | xargs --no-run-if-empty kubectl delete {{ item.kind }} --ignore-not-found
  loop:
    - { kind: PersistentVolume, name: "{{ mysql_name }}" }
  failed_when: false
  run_once: true

