#---
- name: 清理 App 资源(有命名空间)
  ansible.builtin.shell: |
    kubectl get {{ item.kind }} -n {{ namespace }} --no-headers -o custom-columns=":metadata.name" \
    | grep -E "^{{ item.name }}" \
    | xargs --no-run-if-empty kubectl delete {{ item.kind }} -n {{ namespace }} --ignore-not-found
  loop:
    - { kind: Deployment, name: "{{ app_name }}" }
    - { kind: StatefulSet, name: "{{ app_name }}" }
    - { kind: ReplicaSet, name: "{{ app_name }}" }
    - { kind: Job, name: "{{ app_name }}" }
    - { kind: Pod, name: "{{ app_name }}" }
    - { kind: Service, name: "{{ app_name }}" }
    - { kind: ConfigMap, name: "{{ app_name }}" }
    - { kind: Secret, name: "{{ app_name }}" }
    - { kind: PersistentVolumeClaim, name: "{{ app_name }}" }
  failed_when: false
  run_once: true


- name: 清理 App 资源(无命名空间)
  ansible.builtin.shell: |
    kubectl get {{ item.kind }} --no-headers -o custom-columns=":metadata.name" \
    | grep -E "^{{ item.name }}" \
    | xargs --no-run-if-empty kubectl delete {{ item.kind }} --ignore-not-found
  loop:
    - { kind: PersistentVolume, name: "{{ app_name }}" }
  failed_when: false
  run_once: true

